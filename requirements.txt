# Verified together on 2026-09-18: Ubuntu 24.04, Python 3.12, RTX 4090 (driver 595, CUDA 13).
# Use Python 3.11 or 3.12. On a machine without an NVIDIA GPU the same pins install the CPU build.
torch==2.14.0
torchvision==0.29.0
torch_geometric==2.8.0.post1
open_clip_torch==3.3.0
faiss-cpu==1.15.1
numpy==2.5.3
pandas==3.0.6
pillow==12.3.0
PyYAML==6.0.3
requests==2.34.2
tqdm==4.70.1
spacy==3.8.16
pytest==9.1.1
